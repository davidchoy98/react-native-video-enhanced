export interface BarProps {
    currentTime: number,
    disabled?: boolean,
    duration: number,
    maximumTrackColor?: string,
    maximumValue?: number,
    minimumTrackColor?: string,
    minimumValue?: number,
    sliderHeight?: number
}